// Catalog stub: Kage build does not use ASCII field variants.
// Preserved to satisfy the exact LandingPages.tsx import seam.
export function buildAsciiFieldDocument(_variant?: string): string | undefined {
  return undefined;
}

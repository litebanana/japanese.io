// Catalog stub: Kage build does not use Betawise globe variants.
// Preserved to satisfy the exact LandingPages.tsx import seam.
export function buildBetawiseGlobeDocument(_variant?: string): string | undefined {
  return undefined;
}

// Catalog stub: Kage uses sourceUrl loading, not srcDoc derivation.
// Preserved to satisfy the exact LandingPages.tsx import seam.
export function buildSandboxedPageDocument(source: string, _options?: unknown): string {
  return source;
}

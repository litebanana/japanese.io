// Catalog stub: Kage build does not use Meridian variants.
// Preserved to satisfy the exact LandingPages.tsx import seam.
export function buildMeridianDocument(_variant?: string, _presentation?: string): string | undefined {
  return undefined;
}

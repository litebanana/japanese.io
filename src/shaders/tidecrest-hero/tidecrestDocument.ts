// Catalog stub: Kage build does not use Tidecrest variants.
// Preserved to satisfy the exact LandingPages.tsx import seam.
export function buildTidecrestDocument(_variant?: string): string | undefined {
  return undefined;
}
